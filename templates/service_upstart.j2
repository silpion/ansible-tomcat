description "Tomcat servlet container"

start on runlevel [2345]
stop on runlevel [!2345]
respawn
respawn limit 10 5

setuid {{ tomcat_user }}
setgid {{ tomcat_group }}

env JAVA_HOME={{ ansible_local.java.general.java_home }}
env CATALINA_HOME={{ tomcat_home }}/catalina
env CATALINA_BASE={{ tomcat_home }}/catalina

script
  exec {{ tomcat_home }}/catalina/bin/catalina.sh run
end script

post-stop script
  rm -rf {{ tomcat_home }}/catalina/temp/*
end script
